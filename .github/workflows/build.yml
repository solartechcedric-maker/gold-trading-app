name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y openjdk-17-jdk
        pip install buildozer cython==0.29.33

    - name: Build APK with Buildozer
      run: |
        buildozer android debug

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: gold-trading-pro-apk
        path: bin/*.apk
```

4. Click **"Commit changes"**

---

## **Step 4: Watch It Build!**

1. Go to the **"Actions"** tab at the top of your repository
2. You'll see a workflow running (yellow dot)
3. Click on it to watch the build progress
4. **Wait 30-45 minutes** for first build

---

## **Step 5: Download Your APK**

Once the build completes (green checkmark ✅):

1. Click on the completed workflow
2. Scroll down to **"Artifacts"** section
3. Click **"gold-trading-pro-apk"** to download
4. Unzip the downloaded file
5. Install the APK on your Android phone!

---

## **Future Updates - Super Easy!**

To update your app later:
1. Go to your repository
2. Click on `main.py`
3. Click the pencil icon (✏️) to edit
4. Make changes
5. Click "Commit changes"
6. GitHub automatically rebuilds the APK!

---

## **Troubleshooting**

If the build fails on GitHub Actions:

1. Go to the **Actions** tab
2. Click the failed workflow (red X)
3. Click on the "build" job
4. Screenshot the error and send it to me

---

## **Visual Guide Summary:**
```
1. GitHub.com → Sign up/Login
2. Click "+" → "New repository"
3. Name it "gold-trading-app"
4. Create repository
5. Add file → Create "main.py" → Paste code → Commit
6. Add file → Create "buildozer.spec" → Paste code → Commit
7. Add file → Create ".github/workflows/build.yml" → Paste code → Commit
8. Go to "Actions" tab → Watch build
9. Download APK from Artifacts
